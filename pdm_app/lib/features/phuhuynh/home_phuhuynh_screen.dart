import 'package:flutter/material.dart';

class HomePhuhuynhScreen extends StatelessWidget {
  const HomePhuhuynhScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Trang chủ Admin')),
    );
  }
}